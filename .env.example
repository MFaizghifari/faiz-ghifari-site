# Google Analytics 4 Measurement ID. Format: G-XXXXXXXXXX
# Set this in Vercel → Settings → Environment Variables (Production only).
# Leave unset locally so dev traffic doesn't pollute analytics.
NEXT_PUBLIC_GA_ID=
